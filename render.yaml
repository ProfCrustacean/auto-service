services:
  - type: web
    name: auto-service-foundation
    runtime: node
    plan: starter
    region: frankfurt
    autoDeployTrigger: off
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
